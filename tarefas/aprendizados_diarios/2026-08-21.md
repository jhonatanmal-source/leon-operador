# Aprendizados Diários — 2026-08-21

## Erro

- ## Missão: Diagnóstico "por que não executa ordens na demo" (somente leitura)

### Cadeia causal confirmada (4 bloqueios empilhados)
1. **BUG latente (commit inicial 857e36a)**: `entry_price_engine.calcular_entrada()` chama `refine_m15_m5(direcao)` sem símbolo → default `XAUUSD` (`mt5_execution_refiner.py:139`) → corretora só tem `Gold_Spot` (XAUUSD = 0 candles) → `INSUFFICIENT_EXECUTION_CANDLES` sempre → path normal de entrada nunca produz entrada (187/211 ciclos em 21/08). Correção proposta: propagar `ativo` até `refine_m15_m5`.
2. **Por design pós-d1e6ba3 (pendência #10)**: zonas LAB nascem `AGUARDANDO_ESTRUTURA`; `validate_zone_for_execution` exige `CONFIRMADA` → `REGION_NOT_CONFIRMED` (24/211 ciclos). As 62 zonas LAB do arquivo estão todas eternamente `AGUARDANDO_ESTRUTURA` — não existe mecanismo de promoção. Follow-up documentado no handoff.
3. **Estado**: `AUTONOMY_EXPIRED` desde 21/08 14:30 (renovação manual por design, pendência #5).
4. **Gates operacionais legítimos**: FIBONACCI_OU_LIQUIDEZ_NAO_CONFIRMADOS (195×, estrutura ONDA 5 CONCLUIDA) + TOP_DOWN_M15_NOT_ALIGNED (24×).

### Achado colateral
Última tentativa real (18/08 10:41, PREOP-003551) morreu em `INVALID_SELL_PRICE_STRUCTURE`: RR planejado 3.0 vs live 36.41 (risk 1.36 / reward 49.52) — validar `build_smc_trade_levels`/preço de execução vs gatilho em missão futura.

### Padrão identificado
Defaults de símbolo hardcoded (`XAUUSD`) em funções de execução são armadilha: esta corretora usa `Gold_Spot`. Verificador: grep `XAUUSD` em defaults de assinatura; sempre propagar `detectar_ativo()`.

- ## Correção A (bug B1) — símbolo propagado na cadeia de entrada

**Arquivos**: `src/entry_price_engine.py` (`calcular_entrada` ganhou param `symbol=None`, repassa a `refine_m15_m5`), `src/leon.py:660` (caller passa `symbol=ativo`), `tests/test_execution_refiner.py` (+4 testes).

**Bug**: `calcular_entrada` chamava `refine_m15_m5(direcao)` sem símbolo → default `XAUUSD` (`mt5_execution_refiner.py:139`) → corretora só tem `Gold_Spot` → `INSUFFICIENT_EXECUTION_CANDLES` em 100% dos ciclos → path normal de entrada nunca produzia entrada.

**Validação real**: `refine_m15_m5('COMPRA')` default = False/INSUFFICIENT; `refine_m15_m5('COMPRA', symbol='Gold_Spot')` = ok, 160 M15 / 180 M5. Suíte: 403 passed (era 400 + 3 novos efetivos).

**Compatibilidade**: sem `symbol`, mantém default `XAUUSD` (nenhum outro caller quebra). Nada de estratégia/risco/TP/SL alterado. Sem commit (aguardando autorização).

## Investigação D (read-only) — INVALID_SELL_PRICE_STRUCTURE
Guard `_rr_no_preco_execucao` (`mt5_order_executor.py:633`) está **correto**: recusou plano degenerado da PREOP-003551 (18/08 10:41) onde stop ficava a 1.36 pts da entrada com estrutura invertida no preço live. A anomalia (RR planejado 3.0 vs live 36.41) nasce **upstream** em `build_smc_trade_levels`/níveis SMC — fora do escopo aprovado (A+D), NÃO alterado. Follow-up recomendado: auditar coerência entre entry/stop/tp planejados vs gatilho M5.

## Lembrete de contrato
B1 sozinho não retoma execução: permanecem B2 (zonas LAB sem promoção a CONFIRMADA) e B3 (autonomia expirada 21/08 14:30). Renovar autonomia só faz sentido após decidir B2.
