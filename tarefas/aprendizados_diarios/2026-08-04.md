# Aprendizados Diários — 2026-08-04

## Operacao

- ## Acompanhamento pós-correções C1-C8 — Operações do dia + Manutenção do ciclo de aprendizado

### Operações Analisadas
- SHADOW-000049 (COMPRA, 4063.59, aberta 00:00) — LOSS

### Resultado do Dia (parcial)
- 1 operação: 0W / 1L
- Streak acumulado: 4 losses consecutivos (000046 → 000049) — a 1 loss do limite de pausa (5).

### Decisão
- **MISSION-20260804-APRENDIZADO**: regularização do sistema de aprendizado diário.
- Identificada e corrigida a causa raiz das 16+ duplicatas de PREOP-000116 no CONTEXTO_EVOLUCAO.md e 42 linhas duplicadas no diário 30/07 (vault Obsidian): `obsidian_sync.py` fazia append sem idempotência no CONTEXTO, que é regenerado integralmente por `daily_learning_sync.py`.
- Correção aplicada: `update_daily_learning()` idempotente; `sync_closed_trade()` não chama mais `update_contexto_evolucao()`; função mantida como DEPRECATED idempotente.
- 6 novos testes de idempotência criados e passando (18/18 nos módulos de sincronização).

### Erros Encontrados
- **Append sem idempotência** em `update_daily_learning()` e `update_contexto_evolucao()` (src/obsidian_sync.py) — permitia duplicação de operações quando `sync_closed_trade()` era chamado repetidamente para a mesma operação (PREOP-000116 x16+).
- **Duplo escritor no CONTEXTO_EVOLUCAO.md**: `obsidian_sync` (append) e `daily_learning_sync` (regravação integral) — conflito de ownership.
- **Lacuna de aprendizados**: nenhum registro diário entre 31/07 e 03/08 apesar de operações nesses dias.

### Correções Aplicadas
| Data | Arquivo | Correção |
|------|---------|----------|
| 2026-08-04 | `src/obsidian_sync.py` | `update_daily_learning()` idempotente (retorna False se entrada já existe) |
| 2026-08-04 | `src/obsidian_sync.py` | `sync_closed_trade()` não chama mais `update_contexto_evolucao()` |
| 2026-08-04 | `src/obsidian_sync.py` | `update_contexto_evolucao()` DEPRECATED + idempotente |
| 2026-08-04 | `tarefas/aprendizados_diarios/CONTEXTO_EVOLUCAO.md` | 16 linhas duplicadas PREOP-000116 removidas |
| 2026-08-04 | `obsidian_vault/aprendizados_diarios/CONTEXTO_EVOLUCAO.md` | Sincronizado com versão limpa |
| 2026-08-04 | `obsidian_vault/aprendizados_diarios/2026-07-30.md` | 42 linhas duplicadas PREOP removidas (sincronizado com tarefas) |
| 2026-08-04 | `tests/test_obsidian_sync_idempotency.py` | 6 testes de regressão criados |

### Padrões Identificados
- **Padrão estrutural promovido**: arquivos de aprendizado gerados/regenerados por um processo não devem receber append de outro processo — single-writer por arquivo. Padrão similar ao já registrado para erros de config permanente (salvar estado para evitar flood).
- **Padrão estrutural promovido**: CONTEXTO_EVOLUCAO.md agora marcado como `CURADO_MANUALMENTE` — o sync NÃO regrava arquivos curados manualmente (proteção adicionada após o sync ter destruído a curadoria na execução de teste).
- **SEGURANÇA**: credenciais em texto plano em diários de aprendizado viram histórico git. `grep -S` em commits de aprendizado é o vetor real de vazamento — nunca registrar senha/token em diários.
- Streak de 4 losses — sistema está a 1 loss do trigger de pausa automática (C5).

### Recomendações
- Verificar amanhã se o streak detector (C5) pausa na 5ª loss consecutiva.
- Registrar diários diariamente a partir de agora — não deixar lacunas.
- Após 20-30 operações pós-C1-C8, reavaliar winrate e decidir filtro de entrada em zona.
- **[SEGURANÇA - ANTES DE QUALQUER PUSH]** As senhas do usuário `jhonatan` (temporária e definitiva) foram committadas no histórico git local (commit `1a78157`) e removidas do working tree em 04/08. O commit é LOCAL APENAS (nunca pusheado). Antes do próximo push: (1) reescrever histórico dos commits locais `1a78157` e `f7e5ce4` (git filter-repo) para purgar as senhas; (2) rotacionar a senha do usuário `jhonatan`; (3) adicionar `obsidian_vault/.trade_stats.json` ao `.gitignore`; (4) instalar gitleaks pré-commit.
- **[SEGURANÇA - stats]** `.trade_stats.json` continua corrompido (42/42/0 = winrate 100% impossível) — follow-up para tornar `save_trade_note()` idempotente nos stats e corrigir o arquivo.
- ## Correção: Idempotência no sistema de aprendizado (obsidian_sync + daily_learning_sync)

**Problema**: `src/obsidian_sync.py` fazia append sem idempotência no CONTEXTO_EVOLUCAO.md (16+ duplicatas PREOP-000116) e no diário diário (42 linhas duplicadas no vault). Causa raiz: dois escritores no mesmo arquivo — obsidian_sync (append) vs daily_learning_sync (regravação integral).

**Correções aplicadas**:
1. `update_daily_learning()` idempotente (retorna False se a linha já existe)
2. `sync_closed_trade()` NÃO chama mais `update_contexto_evolucao()`
3. `update_contexto_evolucao()` DEPRECATED + idempotente
4. `_extrair_secao()` aceita h3 (`### `) além de h2 (`## `) — antes extraía 0 itens dos diários modernos
5. CONTEXTO marcado como `CURADO_MANUALMENTE` — sync não regrava arquivos curados
6. Senhas do usuário removidas do diário 30/07 (estavam em texto plano; presentes no histórico git local `1a78157`, nunca pusheadas)

**Testes**: 8 testes novos (6 idempotência obsidian_sync + 2 daily_learning_sync h3/curadoria), 20/20 passando.

**Padrão promovido**: single-writer por arquivo gerado; nunca registrar credenciais em diários de aprendizado.
