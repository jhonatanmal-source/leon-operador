# Aprendizados Diários — 2026-07-23

## Missões executadas
- Instalação do Obsidian headless (obsidian-headless v0.0.13) para memória auxiliar do LEON
- Criação do vault Obsidian em `/opt/leon/app/obsidian_vault/`
- Estrutura inicial: pastas de aprendizado, operacional, análise, referências, reuniões
- Script de integração com sistema de aprendizado diário existente
- Sincronização bidirecional entre `obsidian_vault/` e `tarefas/aprendizados_diarios/`
- **Criação de 4 MCPs (Model Context Protocol servers) para o LEON:**
  - Memory MCP: acesso ao vault Obsidian, contexto e aprendizado diário (6 tools)
  - Market MCP: coleta de dados de mercado com wrapper read-only (7 tools)
  - Backtest MCP: execução e comparação de backtests (4 tools)
  - Replay MCP: reprodução de operações passadas (5 tools)
- Criação do protocolo base MCP (`mcp_protocol.py`) com JSON-RPC 2.0 sobre stdio
- Criação do wrapper de segurança MT5 (`_mt5_safe.py`) bloqueando funções de escrita
- Registro dos MCPs em `opencode.json`
- **Criação do Telegram Commands MCP** (`src/telegram_commands_mcp.py`):
  - Bot de comandos via long-polling (sem webhook)
  - 6 comandos: /start, /help, /memory, /market, /backtest, /replay
  - Comandos com argumentos: /memory busca <termo>, /backtest executar <dias>, /replay <ID>
  - Formatação Markdown para respostas
  - Comandos registrados na API Telegram via setMyCommands
- **Correção dos imports MCP**: adicionado `src/mcp/` ao sys.path para permitir import como módulo

## Decisões tomadas
- Usar obsidian-headless (CLI oficial) em vez de app GUI (servidor headless)
- Vault Obsidian como complemento, não substituto, do sistema de aprendizado diário
- Sincronização bidirecional entre `obsidian_vault/aprendizados_diarios/` e `tarefas/aprendizados_diarios/`
- Script de integração em `scripts/sync_obsidian_vault.sh` para manter ambos atualizados
- **MCPs em `src/mcp/`** — diretório dedicado, sem poluir src/ raiz
- **Wrapper `_mt5_safe.py`** para isolar funções read-only do MT5 e bloquear ordens
- **MCPs orquestram, não implementam** — Backtest MCP chama engines existentes
- **`opencode.json` no projeto** para registrar MCPs sem modificar config global

## Padrões identificados
- obsidian-headless é a forma correta de integrar Obsidian em servidores headless
- npm install local (sem sudo) funciona perfeitamente para pacotes com build nativo
- better-sqlite3 compila sem problemas no Ubuntu 24.04
- obsidian-headless fornece binário `ob` com comandos de sync, publish e gerenciamento de vault
- **MCPs com stdio + JSON-RPC 2.0** — protocolo leve, sem dependências externas
- **Subclasses MCP** devem aceitar `*args, **kwargs` no `__init__` para compatibilidade com `run_server`
- **Wrapper read-only** para MT5 é essencial para segurança — bloquear `order_send`, `login`, `eval`

## Erros encontrados / Correções aplicadas
- Nenhum erro durante a instalação
- 0 vulnerabilidades no npm audit
- Nenhuma alteração em src/ (código operacional), config/ (configurações) ou data/ (dados)
- **Bug corrigido**: `run_server` passava argumentos posicionais para handlers que não os aceitavam → adicionado `*args, **kwargs` nos `__init__` dos 4 handlers

## Recomendações
- Adicionar o vault ao .gitignore (já feito)
- Considerar Obsidian Sync para backup remoto
- Expandir o vault com notas de análise de mercado
- Integrar o script de sincronização no cron/crontab para execução periódica
- Pode-se usar `npx ob` para gerenciar o vault via CLI
- **Telegram Commands**: iniciar com `python3 src/telegram_commands_mcp.py` para ativar o bot de comandos
- **Considerar systemd --user** para manter o bot de comandos rodando continuamente

## Contratos Protegidos
- Conta real bloqueada ✅
- Nenhum agente enviou ordens MT5 ✅
- Nenhum agente removeu guards ✅
- Estratégia, risco, TP, SL não foram alterados ✅
- Nenhum arquivo existente em src/ foi modificado ✅
- Nenhuma alteração em config/ ou data/ ✅
