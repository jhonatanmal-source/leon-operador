# Aprendizados Diários — 2026-08-13

## Correcao

- ## Missão MISSION-20260813-REVISAO-ALTERACOES

### Erro
- Credencial do usuário `jhonatan` (senha admin + temporária) estava em texto claro em 3 arquivos NÃO commitados: `tarefas/agent_lock.json` (campo `notes`), `tarefas/aprendizados_diarios/2026-08-05.md`, `obsidian_vault/aprendizados_diarios/2026-08-05.md`.
- Causa: ao documentar a MISSION-20260805-GIT-HISTORY (que existia para remover a senha do histórico), o relato citou o valor real da senha em vez de um placeholder. O `.md` já usava `***ROTACIONAR-SENHA***` na descrição do procedimento, mas não na linha que descrevia o achado.
- Risco: commitar o working tree reintroduziria no repositório o segredo que a `filter-branch` de 05/08 havia removido. Confirmado por `git log --all -S`: a string estava ausente de todo o histórico.

### Correcao
- Os 3 arquivos foram mascarados com `***ROTACIONAR-SENHA***` (somente mascaramento; nenhum commit).
- Validado: `grep -rl` no working tree (excluindo `.git`) retorna vazio; `git log --all -S` continua vazio; `agent_lock.json` segue JSON válido; 356 testes passando.

### Padrao
- **Nunca citar o valor de um segredo ao documentar sua remoção.** Relatos de incidente de credencial devem usar `***ROTACIONAR-SENHA***` ou referenciar a chave pelo nome. Verificador: antes de qualquer `git add`, rodar `grep -rl` das credenciais conhecidas no working tree — o histórico limpo não protege contra reintrodução por arquivo não commitado.
- **Segredo exposto + rotação pendente = severidade crítica.** A senha do `jhonatan` continua não rotacionada desde 30/07, então o valor exposto era credencial válida, não segredo morto.

### Operacao
- Revisão somente leitura do diff não commitado: 7 arquivos modificados (637+/97-), ~380 não rastreados (majoritariamente PREOP em `reports/operations/`). Zero alteração em `src/`, `tests/` ou `web_app/`.
- Correção de contagem: o número real de não rastreados é ~380, não 23 (leitura anterior baseada em saída truncada). Verificador: usar `git ls-files --others --exclude-standard`, não `git status --porcelain | head`.
- Parecer emitido: REPROVADO (bloqueador único: exposição de segredo). Após mascaramento, o bloqueador está resolvido; permanecem 3 condições para liberar commit.

### Erro
- **`pytest` na raiz do projeto quebra**: coleta entra em `backups/` (symlink para `/opt/leon/backups/`) e caminha por ~40 níveis de aninhamento recursivo `leon_backup_20260805/backups/leon_backup_20260805/...`, resultando em `INTERNALERROR`. Indica loop de symlink dentro do backup — investigar em missão separada (afeta tamanho e tempo de restauração do backup, além do pytest).
- **`tests/test_leon_brain.py:159` ainda usa `sys.exit(0)`** no nível do módulo, derrubando a coleção de toda a suíte com `INTERNALERROR: SystemExit`. Erro já documentado em `CONTEXTO_EVOLUCAO.md` e ainda não corrigido. Consequência: o "356/356 passando" citado no handoff só é verdade com `--ignore=tests/test_leon_brain.py` — um arquivo de teste está de fato fora da suíte.

### Padrao
- Comando real para rodar a suíte hoje: `/opt/leon/venv/bin/python -m pytest tests/ -q -p no:cacheprovider --ignore=tests/test_leon_brain.py` → 356 passed. Não usar `pytest` na raiz.
- **Append sem rotação em `reports/daily_learning_report.txt`**: 1418 linhas, 17 blocos acumulados (15/06 a 11/08) com duplicatas (`2026-06-16` 7x, `2026-06-15` 3x). Mesmo padrão que gerou as duplicatas PREOP-000116 em 04/08 — a regra "single-writer por arquivo gerado" existe em `CONTEXTO_EVOLUCAO.md` e não está sendo respeitada por este arquivo.
- **Perda de evidência auditável em `handoff_atual.md`**: o diff descartou 69 linhas e escreveu 32, removendo a matriz de auditoria de segurança (9 itens com evidência) e o pipeline de 11 guards. Handoff não deve sobrescrever evidência de auditoria — arquivar em `tarefas/relatorios/` antes de reescrever.

### Decisao
- Contratos preservados e verificados: `demo_only = true`, guard `MT5_REAL_ACCOUNT_BLOCKED` intacto, nenhum guard removido, nenhuma ordem enviada, estratégia/risco/TP/SL não alterados. Nenhum commit realizado.
- Pendente de decisão do usuário: `learning_lab_enabled = true` desativa SMC guard, top-down e conselho de operadores (achado da auditoria anterior; `config.ini` está no `.gitignore`, mtime 30/07, sem rastro em git).


